<div class="card">
    <div class="card-body">
        <h1 class="card-title">{{title}}</h1>
        <p class="card-text">{{user.username}}</p>
        <h2 class="card-text">{{content}}</h2>

    </div>

    <div class="card-footer">
        <p> {{format_date date}} {{formate_time date}}</p>
       </div>

</div>

{{>comment-section}}